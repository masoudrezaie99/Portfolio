const Divider = (props) => {
    const title = props.title
    return ( 

                <div className="divider">
                    <span>{title}</span>
                </div>

     );
}
 
export default Divider;